---
const { class: className, id} = Astro.props
---
<section
    id={id}
    data-section={id}
    class={`section ${className} mx-auto container lg:max-w-4xl flex flex-col`}
>
    <slot />
</section>